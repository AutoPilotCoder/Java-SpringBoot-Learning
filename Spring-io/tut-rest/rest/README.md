# Improvements

1. RESTful
2. HyperText (Return links to other available functions in basic CRUD Functions)
3. Assemblers that use links to the self class functions so links are automatically updated
3. Link (Dynamic HyperText based on data type in assembler (remove link available when status is inprogress))